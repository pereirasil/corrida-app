{
  "name": "corrida-app-backend",
  "version": "1.0.0",
  "description": "Backend para o app de corrida social com GPS, música sincronizada e corridas em grupo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:setup": "psql -h localhost -U postgres -d postgres -f database/schema.sql",
    "db:reset": "psql -h localhost -U postgres -d postgres -c \"DROP SCHEMA public CASCADE; CREATE SCHEMA public;\" && npm run db:setup"
  },
  "keywords": [
    "corrida",
    "running",
    "gps",
    "social",
    "music",
    "fitness",
    "api",
    "postgresql",
    "express"
  ],
  "author": "Anderson Pereira",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.3",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
